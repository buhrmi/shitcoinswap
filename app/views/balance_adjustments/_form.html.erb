<%= form_with(model: balance_adjustment, local: true) do |form| %>
  <% if balance_adjustment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(balance_adjustment.errors.count, "error") %> prohibited this balance_adjustment from being saved:</h2>

      <ul>
      <% balance_adjustment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :coin_id %>
    <%= form.number_field :coin_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :change_type %>
    <%= form.text_field :change_type %>
  </div>

  <div class="field">
    <%= form.label :change_id %>
    <%= form.number_field :change_id %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_field :memo %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
