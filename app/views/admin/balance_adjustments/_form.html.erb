<%= form_with(model: [:admin, balance_adjustment], local: true) do |form| %>
  <% if balance_adjustment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(balance_adjustment.errors.count, "error") %> prohibited this balance_adjustment from being saved:</h2>

      <ul>
      <% balance_adjustment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :coin_id %>
    <%= form.select :coin_id, Coin.all.map { |coin| [coin.symbol, coin.id] } %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select :user_id, User.all.map { |user| [user.email, user.id] } %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_field :memo %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
