<p id="notice"><%= notice %></p>

<h1>Balance Adjustments</h1>

<table>
  <thead>
    <tr>
      <th>Asset</th>
      <th>User</th>
      <th>Change type</th>
      <th>Change</th>
      <th>Memo</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @balance_adjustments.each do |balance_adjustment| %>
      <tr>
        <td><%= balance_adjustment.asset_id %></td>
        <td><%= balance_adjustment.user_id %></td>
        <td><%= balance_adjustment.change_type %></td>
        <td><%= balance_adjustment.change_id %></td>
        <td><%= balance_adjustment.memo %></td>
        <td><%= balance_adjustment.amount %></td>
        <td><%= link_to 'Show', admin_balance_adjustment_path(balance_adjustment) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Balance Adjustment', new_admin_balance_adjustment_path %>
