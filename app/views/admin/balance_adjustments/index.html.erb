<p id="notice"><%= notice %></p>

<h1><%= t '.balance' %></h1>

<table>
  <thead>
    <tr>
      <th><%= t '.asset_2' %></th>
      <th><%= t '.user_1' %></th>
      <th><%= t '.change_type' %></th>
      <th><%= t '.change' %></th>
      <th><%= t '.memo' %></th>
      <th><%= t '.amount_1' %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @balance_adjustments.each do |balance_adjustment| %>
      <tr>
        <td><%= balance_adjustment.asset_id %></td>
        <td><%= balance_adjustment.user_id %></td>
        <td><%= balance_adjustment.change_type %></td>
        <td><%= balance_adjustment.change_id %></td>
        <td><%= balance_adjustment.memo %></td>
        <td><%= balance_adjustment.amount %></td>
        <td><%= link_to (t '.show'), admin_balance_adjustment_path(balance_adjustment) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to (t '.new_balance'), new_admin_balance_adjustment_path %>
